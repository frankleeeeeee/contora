; Prefer real copies for file: deps when npm honors it (Windows may still use a junction).
install-links=false
; vsce: use `npm run vsix` which passes --follow-symlinks so junctions to packages/runtime pack correctly.
